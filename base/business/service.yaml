apiVersion: v1
kind: Service
metadata:
  name: business
  namespace: amuse
  labels:
    app.kubernetes.io/name: business
    app.kubernetes.io/part-of: amuse
spec:
  selector:
    app.kubernetes.io/name: business
  ports:
    - name: http
      port: 3001
      targetPort: http
