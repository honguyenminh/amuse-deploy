apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: amuse
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: amuse
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
