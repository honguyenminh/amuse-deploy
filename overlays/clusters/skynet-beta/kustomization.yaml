apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../dev

configMapGenerator:
  - name: amuse-cluster-config
    behavior: replace
    envs:
      - cluster.env
    options:
      disableNameSuffixHash: true
