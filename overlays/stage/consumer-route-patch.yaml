# Consumer is on the zone apex (amuse.m8.io.vn), not under the *.amuse wildcard listener.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: amuse-consumer
  namespace: amuse
spec:
  parentRefs:
    - name: amuse-gateway
      sectionName: https-apex
