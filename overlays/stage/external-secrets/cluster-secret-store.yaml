apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: amuse-azure-kv
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: "https://REPLACE_KV_NAME.vault.azure.net"
      serviceAccountRef:
        name: amuse-api
        namespace: amuse
